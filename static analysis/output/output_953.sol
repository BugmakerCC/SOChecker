pragma solidity ^0.8.9;
contract BridgeAdded {

    event BridgeAdded(
        uint256 indexed id,
        string tokenTicker,
        string tokenName,
        string imageUrl
    );

    event BridgeAdded(
            string indexed indexedTokenTicker,
            string tokenTicker,
            string tokenName,
            string imageUrl
        );
}

