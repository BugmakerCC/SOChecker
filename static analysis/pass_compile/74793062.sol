pragma solidity >=0.7.0 <0.9.0;

contract FinalExcercise {
    uint256 a = 300;
    uint256 b = 12;
    uint256 f = 47;

    function finalize() public view returns (uint256) {
        uint256 d = 23;
        if (a <= a && b < f) {
            return (d * 2) - b;
        }
    }
}

